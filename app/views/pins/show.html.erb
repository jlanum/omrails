<div class="product-table-show">
<table class="table table-bordered table-striped">
              <thead>
                <tr>
                  <th>Store</th>
                  <th>Brand</th>
                  <th>Description</th>
									<th>Qty</th>
									<th>Markdown</th>
                </tr>
              </thead>
              <tbody>
                <tr>
	  <td><%= @pin.user.name %></td>
	  <td><%= @pin.brand %></td>
		<td><%= @pin.description %></td>
		<td><%= @pin.quantity %></td>
		<td><%= markprice = @pin.markdown - 42 %></td>
	  </tbody>
  		

	
		
</table>
		<% if current_user = @pin.user %>
	<%= link_to 'Edit', edit_pin_path(@pin) %> |
		<% end %>
	<%= link_to 'Back', pins_path %>	
	
	
	
</div>

