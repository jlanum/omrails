<div class="product-table3">
<table class="table table-bordered table-striped">
              <thead>
                <tr>
                  <th>Store</th>
                  <th>Brand</th>
                  <th>Description</th>
									<th>Price</th>
									<th>Markdown</th>
									<th>Markdown First</th>
                </tr>
              </thead>
              <tbody>
                <tr>
	  <td><%= @pin.user.name %></td>
	  <td><%= @pin.brand %></td>
		<td><%= @pin.description %></td>
		<td><%= number_to_currency(@pin.price) %></td>
		<td><%= @pin.markdown %></td>
		<td><%= markprice = @pin.markdown + 27 %></td>
	  </tbody>
  		

	
		
</table>
		<% if current_user = @pin.user %>
	<%= link_to 'Edit', edit_pin_path(@pin) %> |
		<% end %>
	<%= link_to 'Back', pins_path %>	
	
	
	
</div>

